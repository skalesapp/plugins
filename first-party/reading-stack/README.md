# Reading Stack

A reading list that works offline - save links, let the agent summarize.

**Placeholder.** The plugin content in this directory is synced from the Skales
app bundle; it is not authored here. The definition ships with the app and is
mirrored into this repository so the registry entry has a source of record.

Do not edit files in this directory by hand - a sync overwrites them.

- Class: `page`
- Platforms: desktop
- Registry entry: `com.github.skalesapp/reading-stack` in [`plugins.json`](../../plugins.json)
